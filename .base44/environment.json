{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "OPENAI_API_KEY",
      "description": "OpenAI API key (sk-...) that powers the AI coach chat and AI workout-plan generation via litellm. Optional — app boots without it; AI endpoints return 503 until set. Obtain at https://platform.openai.com/api-keys (account must have billing/quota).",
      "requiredAtBoot": false
    }
  ]
}
